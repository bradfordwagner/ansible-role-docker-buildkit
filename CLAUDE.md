# bradfordwagner.ansible-role-docker-buildkit

## Updating the version

Version is controlled by `bk_ver` in `defaults/main.yml`.

Check for the latest release at: https://github.com/moby/buildkit/releases

```yaml
bk_ver: vX.Y.Z
```
